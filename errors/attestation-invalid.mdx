---
title: "Attestation invalid"
description: "Recreate platform evidence from the active challenge; do not reuse evidence."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code attestation_invalid. */}

`attestation_invalid` is a stable Latchway error code. It maps to HTTP `401` and
is not automatically retryable.

## What to do

Recreate platform evidence from the active challenge; do not reuse evidence.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/attestation-invalid",
  "documentation_url": "https://docs.latchway.dev/errors/attestation-invalid",
  "title": "Attestation invalid",
  "status": 401,
  "detail": "Attestation invalid.",
  "code": "attestation_invalid",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
