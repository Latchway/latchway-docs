---
title: "ETag required"
description: "Resubmit the mutation with the current If-Match value."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code etag_required. */}

`etag_required` is a stable Latchway error code. It maps to HTTP `428` and
is not automatically retryable.

## What to do

Resubmit the mutation with the current If-Match value.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/etag-required",
  "documentation_url": "https://docs.latchway.dev/errors/etag-required",
  "title": "ETag required",
  "status": 428,
  "detail": "ETag required.",
  "code": "etag_required",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
