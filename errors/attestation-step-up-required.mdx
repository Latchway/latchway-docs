---
title: "Stronger attestation required"
description: "Complete the stronger trust flow required by the feature policy."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code attestation_step_up_required. */}

`attestation_step_up_required` is a stable Latchway error code. It maps to HTTP `401` and
is not automatically retryable.

## What to do

Complete the stronger trust flow required by the feature policy.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/attestation-step-up-required",
  "documentation_url": "https://docs.latchway.dev/errors/attestation-step-up-required",
  "title": "Stronger attestation required",
  "status": 401,
  "detail": "Stronger attestation required.",
  "code": "attestation_step_up_required",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
