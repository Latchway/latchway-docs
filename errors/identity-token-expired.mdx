---
title: "Identity token expired"
description: "Refresh or reacquire the application identity token."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code identity_token_expired. */}

`identity_token_expired` is a stable Latchway error code. It maps to HTTP `401` and
is not automatically retryable.

## What to do

Refresh or reacquire the application identity token.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/identity-token-expired",
  "documentation_url": "https://docs.latchway.dev/errors/identity-token-expired",
  "title": "Identity token expired",
  "status": 401,
  "detail": "Identity token expired.",
  "code": "identity_token_expired",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
