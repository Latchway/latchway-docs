---
title: "ETag mismatch"
description: "Fetch the latest revision, merge changes and resubmit with its ETag."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code etag_mismatch. */}

`etag_mismatch` is a stable Latchway error code. It maps to HTTP `412` and
is not automatically retryable.

## What to do

Fetch the latest revision, merge changes and resubmit with its ETag.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/etag-mismatch",
  "documentation_url": "https://docs.latchway.dev/errors/etag-mismatch",
  "title": "ETag mismatch",
  "status": 412,
  "detail": "ETag mismatch.",
  "code": "etag_mismatch",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
