---
title: "Operation outcome indeterminate"
description: "Preserve operation_id. Only a correlated succeeded audit event proves the original request committed; current state or a later conflict is insufficient. Reconcile the audit result before retrying, except for an explicitly documented idempotent operation."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code operation_indeterminate. */}

`operation_indeterminate` is a stable Latchway error code. It maps to HTTP `503` and
is retryable only under the bounded guidance below.

## What to do

Preserve operation_id. Only a correlated succeeded audit event proves the original request committed; current state or a later conflict is insufficient. Reconcile the audit result before retrying, except for an explicitly documented idempotent operation.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/operation-indeterminate",
  "documentation_url": "https://docs.latchway.dev/errors/operation-indeterminate",
  "title": "Operation outcome indeterminate",
  "status": 503,
  "detail": "Operation outcome indeterminate.",
  "code": "operation_indeterminate",
  "request_id": "req_...",
  "retryable": true
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
