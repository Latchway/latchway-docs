---
title: "Direct component attestation required"
description: "Complete the component definition's direct platform attestation flow."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code component_direct_attestation_required. */}

`component_direct_attestation_required` is a stable Latchway error code. It maps to HTTP `401` and
is not automatically retryable.

## What to do

Complete the component definition's direct platform attestation flow.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/component-direct-attestation-required",
  "documentation_url": "https://docs.latchway.dev/errors/component-direct-attestation-required",
  "title": "Direct component attestation required",
  "status": 401,
  "detail": "Direct component attestation required.",
  "code": "component_direct_attestation_required",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
