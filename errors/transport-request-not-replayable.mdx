---
title: "Transport request not replayable"
description: "Recreate the request body before retrying; the transport will not replay a consumed stream."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code transport_request_not_replayable. */}

`transport_request_not_replayable` is a stable Latchway error code. It maps to HTTP `409` and
is not automatically retryable.

## What to do

Recreate the request body before retrying; the transport will not replay a consumed stream.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/transport-request-not-replayable",
  "documentation_url": "https://docs.latchway.dev/errors/transport-request-not-replayable",
  "title": "Transport request not replayable",
  "status": 409,
  "detail": "Transport request not replayable.",
  "code": "transport_request_not_replayable",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
