---
title: "Installation family revoked"
description: "Clear every component session in the family and re-register only when policy permits."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code installation_family_revoked. */}

`installation_family_revoked` is a stable Latchway error code. It maps to HTTP `403` and
is not automatically retryable.

## What to do

Clear every component session in the family and re-register only when policy permits.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/installation-family-revoked",
  "documentation_url": "https://docs.latchway.dev/errors/installation-family-revoked",
  "title": "Installation family revoked",
  "status": 403,
  "detail": "Installation family revoked.",
  "code": "installation_family_revoked",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
