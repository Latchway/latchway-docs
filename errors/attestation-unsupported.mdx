---
title: "Attestation unsupported"
description: "Use a supported platform/provider or contact the application operator."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code attestation_unsupported. */}

`attestation_unsupported` is a stable Latchway error code. It maps to HTTP `400` and
is not automatically retryable.

## What to do

Use a supported platform/provider or contact the application operator.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/attestation-unsupported",
  "documentation_url": "https://docs.latchway.dev/errors/attestation-unsupported",
  "title": "Attestation unsupported",
  "status": 400,
  "detail": "Attestation unsupported.",
  "code": "attestation_unsupported",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
