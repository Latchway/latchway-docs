---
title: "Attestation required"
description: "Complete the configured platform attestation flow."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/error-codes.yaml code attestation_required. */}

`attestation_required` is a stable Latchway error code. It maps to HTTP `401` and
is not automatically retryable.

## What to do

Complete the configured platform attestation flow.

## Correlate the request

Preserve the response `request_id` and use it in the Latchway Console or
CLI request view. Do not log identity tokens, DPoP proofs, attestation
objects, refresh credentials, provider keys, or raw upstream bodies.

## Problem response

```json
{
  "type": "https://docs.latchway.dev/errors/attestation-required",
  "documentation_url": "https://docs.latchway.dev/errors/attestation-required",
  "title": "Attestation required",
  "status": 401,
  "detail": "Attestation required.",
  "code": "attestation_required",
  "request_id": "req_...",
  "retryable": false
}
```

See the [complete error registry](/reference/errors) for retry boundaries
and every other stable code.
