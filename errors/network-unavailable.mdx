---
title: "Native network unavailable"
description: "Check connectivity and the exact configured Latchway origin. Retry only when the exception is explicitly retryable and the request remains replayable."
icon: "circle-alert"
audience: "reference"
pageType: "troubleshooting"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-09-01"
owner: "client-sdks"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/sdk-error-codes.yaml code network_unavailable. */}

`network_unavailable` is a stable SDK-local Latchway error code for Android.
It is not an HTTP Problem and has no canonical HTTP status or global
retryable value.

## What to do

Check connectivity and the exact configured Latchway origin. Retry only when the exception is explicitly retryable and the request remains replayable.

## Retry boundary

Honor the concrete error instance and SDK contract. Never infer that a
local transport failure happened before dispatch, and never replay a
consumed body or an operation with an indeterminate outcome.

## Diagnostic identity

The deterministic documentation URL is `https://docs.latchway.dev/errors/network-unavailable`. Preserve
only redaction-safe request identifiers and error metadata. Do not log
identity tokens, DPoP proofs, attestation objects, refresh credentials,
provider keys, Keychain contents, or request/response bodies.

See the [complete error registry](/reference/errors) for gateway and SDK
error boundaries.
