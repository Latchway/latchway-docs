---
title: "Configuration schema"
description: "Inspect the generated immutable EnvironmentConfig fields, required properties, shapes, bounds, and closed-object rules."
icon: "file-json"
audience: "reference"
pageType: "reference"
serverVersion: "1.0.0"
sdkVersion: "not-applicable"
lastVerified: "2026-08-31"
owner: "core-configuration"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical source: api/config.schema.json; coordinate: https://latchway.dev/schemas/config/1.0.0/environment-config.schema.json. */}

<Warning>
  This generated index helps humans review a candidate document; the
  canonical JSON Schema and authoritative server validation remain decisive.
  Conditional `if`/`then`, reference, CEL, secret, pricing, and runtime
  semantic checks can reject a document that satisfies one property row.
</Warning>

Immutable, revisioned configuration for one Latchway application environment.

## Contract coordinate

| Field | Value |
| --- | --- |
| JSON Schema dialect | `https://json-schema.org/draft/2020-12/schema` |
| Schema ID | `https://latchway.dev/schemas/config/1.0.0/environment-config.schema.json` |
| Root type | `object` |
| Unknown root properties | Rejected |

## Root document

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `apiVersion` | constant `"latchway.dev/v1alpha1"` | Yes | — | — |
| `kind` | constant `"EnvironmentConfig"` | Yes | — | — |
| `metadata` | `Metadata` | Yes | — | — |
| `spec` | `Spec` | Yes | — | — |

The root constants are `apiVersion: latchway.dev/v1alpha1` and
`kind: EnvironmentConfig`. Configuration revisions replace the complete
document under a strong ETag; they are not partial patches.

## Definition index

The canonical schema currently declares 39 reusable definitions.

### Identifier

Shape: string. Definition constraints: pattern: `"^[a-z][a-z0-9_-]{0,62}$"`.

### SecretRef

Shape: string. Definition constraints: pattern: `"^secret/[a-z][a-z0-9_-]{0,62}$"`.

### CELExpression

Shape: string. Definition constraints: minimum length: `1`; maximum length: `4096`.

### Duration

Shape: string. Definition constraints: pattern: `"^[1-9][0-9]*(ms|s|m|h|d)$"`.

### Metadata

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `organization` | `Identifier` | Yes | — | — |
| `application` | `Identifier` | Yes | — | — |
| `environment` | `Identifier` | Yes | — | — |
| `description` | string | No | maximum length: `512` | — |
| `labels` | map to string | No | maximum properties: `32`; property names: pattern: `"^[a-z][a-z0-9_.-]{0,62}$"` | — |

### Spec

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `identityProviders` | array of `IdentityProvider` | Yes | minimum items: `1`; maximum items: `32` | — |
| `attestationPolicies` | array of `AttestationPolicy` | Yes | minimum items: `1`; maximum items: `32` | — |
| `componentDefinitions` | array of `ComponentDefinition` | No | maximum items: `64`; default: `[]` | — |
| `upstreams` | array of `Upstream` | Yes | minimum items: `1`; maximum items: `64` | — |
| `models` | array of `Model` | Yes | minimum items: `1`; maximum items: `256` | — |
| `inputAccountingProfiles` | array of `InputAccountingProfile` | No | maximum items: `256`; default: `[]` | — |
| `pricingCatalogs` | array of `PricingCatalog` | No | maximum items: `64`; default: `[]` | — |
| `limitPlans` | array of `LimitPlan` | Yes | minimum items: `1`; maximum items: `128` | — |
| `features` | array of `Feature` | Yes | minimum items: `1`; maximum items: `256` | — |
| `session` | `SessionPolicy` | No | — | — |
| `privacy` | `PrivacyPolicy` | No | — | — |

### ClaimMapping

Shape: map to `CELExpression`. Definition constraints: maximum properties: `32`; property names: pattern: `"^[a-z][a-z0-9_]{0,62}$"`.

### IdentityProvider

Shape: object. Definition constraints: unknown properties rejected; 7 combined rule(s).

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `type` | enum `"generic_oidc"`, `"firebase"`, `"supabase"`, `"clerk"`, `"custom_jwt"` | Yes | — | — |
| `projectId` | string | No | pattern: `"^[a-z][a-z0-9-]{4,28}[a-z0-9]$"` | — |
| `projectUrl` | string | No | format: `"uri"`; pattern: `"^https://"` | — |
| `issuer` | string | No | format: `"uri"`; pattern: `"^https://"` | — |
| `audiences` | array of string | No | minimum items: `1`; unique items; items: minimum length: `1`; maximum length: `256` | — |
| `authorizedParties` | array of string | No | unique items; items: minimum length: `1`; maximum length: `256` | — |
| `allowedAlgorithms` | array of enum `"RS256"`, `"RS384"`, `"RS512"`, `"ES256"`, `"ES384"`, `"HS256"` | No | minimum items: `1`; unique items | — |
| `jwksUrl` | string | No | format: `"uri"`; pattern: `"^https://"` | — |
| `staticPublicKeySecretRef` | `SecretRef` | No | — | — |
| `symmetricSecretRef` | `SecretRef` | No | — | — |
| `acknowledgeSymmetricRisk` | boolean | No | default: `false` | — |
| `subjectClaim` | string | No | pattern: `"^[A-Za-z][A-Za-z0-9_-]{0,127}(\\.[A-Za-z][A-Za-z0-9_-]{0,127})*$"`; maximum length: `128`; default: `"sub"` | — |
| `clockSkewSeconds` | integer | No | minimum: `0`; maximum: `300`; default: `60` | — |
| `requiredClaims` | array of string | No | maximum items: `32`; unique items; items: pattern: `"^[A-Za-z][A-Za-z0-9_-]{0,127}(\\.[A-Za-z][A-Za-z0-9_-]{0,127})*$"`; maximum length: `128` | — |
| `claimMappings` | `ClaimMapping` | No | — | — |

This definition also has 7 canonical conditional rule(s).
They are enforced in addition to the property summary above.

### AttestationPolicy

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `maxAge` | `Duration` | No | — | — |
| `platforms` | object | Yes | minimum properties: `1`; unknown properties rejected | — |

### ComponentDefinition

Shape: object. Definition constraints: unknown properties rejected; 2 combined rule(s).

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `platform` | enum `"ios"`, `"android"`, `"web"`, `"node"`, `"react_native_ios"`, `"react_native_android"`, `"watchos"`, `"wearos"` | Yes | — | — |
| `kind` | enum `"main_app"`, `"widget"`, `"share_extension"`, `"app_intent_extension"`, `"notification_service_extension"`, `"action_extension"`, `"sso_extension"`, `"watch_extension"`, `"android_app"`, `"wear_app"`, `"browser"`, `"node_process"` | Yes | — | — |
| `identifiers` | `ComponentIdentifiers` | No | — | — |
| `familyRole` | enum `"root"`, `"delegated"` | Yes | — | — |
| `delegation` | `ComponentDelegationPolicy` | No | — | — |
| `attestation` | `ComponentAttestationPolicy` | Yes | — | — |
| `allowedFeatures` | array of `Identifier` | Yes | minimum items: `1`; maximum items: `256`; unique items | — |

This definition also has 2 canonical conditional rule(s).
They are enforced in addition to the property summary above.

### ComponentIdentifiers

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `bundleIdentifiers` | array of string | No | minimum items: `1`; maximum items: `32`; unique items; items: pattern: `"^[A-Za-z0-9][A-Za-z0-9.-]*[A-Za-z0-9]$"`; minimum length: `3`; maximum length: `256` | — |
| `packageNames` | array of string | No | minimum items: `1`; maximum items: `32`; unique items; items: pattern: `"^[A-Za-z][A-Za-z0-9_]*(\\.[A-Za-z][A-Za-z0-9_]*)+$"`; minimum length: `3`; maximum length: `256` | — |
| `origins` | array of string | No | minimum items: `1`; maximum items: `32`; unique items; items: format: `"uri"`; pattern: `"^(?:https://.*\|http://(?:localhost\|127\\.0\\.0\\.1\|\\[::1\\])(?::[0-9]+)?)$"`; maximum length: `2048` | — |

### ComponentDelegationPolicy

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `allowedParents` | array of `Identifier` | Yes | minimum items: `1`; maximum items: `32`; unique items | — |
| `maximumLifetime` | `Duration` | Yes | — | — |
| `allowChildDelegation` | boolean | No | default: `false` | — |

### ComponentAttestationPolicy

Shape: object. Definition constraints: unknown properties rejected; 3 combined rule(s).

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `strategy` | enum `"direct"`, `"delegated"`, `"identity_only"` | Yes | — | — |
| `provider` | enum `"app_attest"`, `"play_integrity"`, `"firebase_app_check"`, `"turnstile"`, `"debug"` | No | — | — |
| `directStepUp` | boolean | No | default: `false` | Allow an eligible Apple delegated component to add direct App Attest evidence for its own component key after provisioning. The component attestation exchange still requires that evidence even though the referenced policy is configured in preferred mode. |
| `directAttestationPolicy` | `Identifier` | No | — | Component-only App Attest policy used by direct step-up. It must use preferred mode, require at least app_verified trust and pin the exact component bundle identifier; it is never an alternative initial-session policy. |

This definition also has 3 canonical conditional rule(s).
They are enforced in addition to the property summary above.

### PlatformAttestation

Shape: object. Definition constraints: unknown properties rejected; 12 combined rule(s).

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `provider` | enum `"app_attest"`, `"play_integrity"`, `"firebase_app_check"`, `"turnstile"`, `"debug"` | Yes | — | — |
| `mode` | enum `"required"`, `"preferred"`, `"disabled"` | Yes | — | — |
| `minimumTrustLevel` | enum `"none"`, `"identity_only"`, `"web_risk_verified"`, `"app_verified"`, `"device_verified"`, `"strong_device_verified"`, `"debug"` | No | — | — |
| `applicationIdentifiers` | array of string | No | unique items; items: minimum length: `1`; maximum length: `256` | — |
| `allowedOrigins` | array of string | No | minimum items: `1`; maximum items: `32`; unique items; items: format: `"uri"`; pattern: `"^(?:https://.*\|http://(?:localhost\|127\\.0\\.0\\.1\|\\[::1\\])(?::[0-9]+)?)$"`; minimum length: `9`; maximum length: `2048` | — |
| `secretRef` | `SecretRef` | No | — | — |
| `dangerousAllowInProduction` | boolean | No | default: `false` | — |
| `appAttest` | `AppAttestConfiguration` | No | — | — |
| `playIntegrity` | `PlayIntegrityConfiguration` | No | — | — |
| `firebaseAppCheck` | `FirebaseAppCheckConfiguration` | No | — | — |
| `turnstile` | `TurnstileConfiguration` | No | — | — |

This definition also has 12 canonical conditional rule(s).
They are enforced in addition to the property summary above.

### AppAttestConfiguration

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `appIdPrefix` | string | Yes | pattern: `"^[A-Z0-9]{1,64}$"` | — |
| `bundleId` | string | Yes | pattern: `"^[A-Za-z0-9](?:[A-Za-z0-9.-]{0,253}[A-Za-z0-9])?$"`; minimum length: `1`; maximum length: `255` | — |
| `environment` | enum `"development"`, `"production"` | Yes | — | — |
| `allowedValidationCategories` | array of enum `1`, `2`, `3`, `4`, `5`, `6`, `10` | Yes | minimum items: `1`; maximum items: `7`; unique items | Exact Apple launch ValidationCategory allowlist: 2 TestFlight, 3 development signing, 4 App Store, 5 ad hoc or enterprise, 6 Developer ID, 10 otherwise classified signing; category 1 is reserved for operating-system executables. |
| `allowedBundleVersions` | array of string | Yes | minimum items: `1`; maximum items: `64`; unique items; items: pattern: `"^[A-Za-z0-9](?:[A-Za-z0-9.-]{0,126}[A-Za-z0-9])?$"`; minimum length: `1`; maximum length: `128` | Exact apple_bundle_version_01 values, sourced from CFBundleVersion/CURRENT_PROJECT_VERSION rather than CFBundleShortVersionString/MARKETING_VERSION. |

### PlayIntegrityConfiguration

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `packageName` | string | Yes | pattern: `"^[A-Za-z][A-Za-z0-9_]*(?:\\.[A-Za-z][A-Za-z0-9_]*)+$"`; minimum length: `3`; maximum length: `255` | — |
| `cloudProjectNumber` | integer | Yes | minimum: `1`; maximum: `9223372036854775807` | — |
| `certificateSha256Digests` | array of string | Yes | minimum items: `1`; maximum items: `16`; unique items; items: pattern: `"^[A-Za-z0-9_-]{43}=?$"` | — |
| `minimumDeviceIntegrity` | enum `"device"`, `"strong"` | Yes | — | — |
| `requireLicensed` | boolean | Yes | — | — |
| `allowTestingResponses` | boolean | Yes | — | — |
| `minimumVersionCode` | integer | Yes | minimum: `0`; maximum: `9223372036854775807` | — |
| `maximumVersionCode` | integer | Yes | minimum: `0`; maximum: `9223372036854775807` | — |
| `credentialSource` | enum `"metadata"`, `"service_account"` | Yes | — | — |

### FirebaseAppCheckConfiguration

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `projectNumber` | string | Yes | pattern: `"^[1-9][0-9]{0,19}$"` | — |
| `allowedAppIds` | array of string | Yes | minimum items: `1`; maximum items: `32`; unique items; items: pattern: `"^[!-~]+$"`; minimum length: `5`; maximum length: `256` | — |

### TurnstileConfiguration

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `allowedHostnames` | array of string | Yes | minimum items: `1`; maximum items: `32`; unique items; items: pattern: `"^[a-z0-9](?:[a-z0-9.-]{0,251}[a-z0-9])?$"`; minimum length: `1`; maximum length: `253` | — |
| `expectedAction` | string | Yes | pattern: `"^[A-Za-z0-9_-]+$"`; minimum length: `1`; maximum length: `32` | — |

### Upstream

Shape: object. Definition constraints: unknown properties rejected; 2 combined rule(s).

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `type` | enum `"openai_compatible"`, `"anthropic"`, `"generic"` | Yes | — | — |
| `baseUrl` | string | Yes | format: `"uri"`; pattern: `"^https?://"` | — |
| `dangerousAllowInsecureHttp` | boolean | No | default: `false` | — |
| `authentication` | `UpstreamAuthentication` | Yes | — | — |
| `timeouts` | `Timeouts` | No | — | — |
| `destinationPolicy` | `DestinationPolicy` | No | — | — |
| `providerReportedCost` | `ProviderReportedCost` | No | — | — |
| `staticHeaders` | map to string | No | maximum properties: `32`; property names: pattern: `"^[A-Za-z0-9!#$%&'*+.^_`\|~-]+$"` | — |

This definition also has 2 canonical conditional rule(s).
They are enforced in addition to the property summary above.

### ProviderReportedCost

Explicit opt-in to exact final provider usage.cost settlement. Configured pricing remains authoritative for pre-dispatch reservations.

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `source` | constant `"openrouter_usage_cost"` | Yes | — | — |
| `currency` | constant `"USD"` | Yes | — | — |

### UpstreamAuthentication

Shape: one of object / object / object / object / object. Definition constraints: —.

### UpstreamAuthenticationHeader

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `headerName` | string | Yes | pattern: `"^[A-Za-z0-9!#$%&'*+.^_`\|~-]+$"`; minimum length: `1`; maximum length: `256` | — |
| `secretRef` | `SecretRef` | Yes | — | — |

### Timeouts

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `connect` | `Duration` | No | — | — |
| `responseHeader` | `Duration` | No | — | — |
| `firstByte` | `Duration` | No | — | — |
| `idle` | `Duration` | No | — | — |
| `total` | `Duration` | No | — | — |

### DestinationPolicy

Shape: object. Definition constraints: unknown properties rejected; 1 combined rule(s).

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `allowedPorts` | array of integer | No | default: `[443]`; unique items; items: minimum: `1`; maximum: `65535` | — |
| `allowRedirects` | boolean | No | default: `false` | — |
| `allowPrivateNetworks` | boolean | No | default: `false` | — |
| `allowedCidrs` | array of string | No | maximum items: `32`; default: `[]`; unique items; items: minimum length: `8`; maximum length: `43` | — |
| `dnsPinning` | boolean | No | default: `true` | — |

This definition also has 1 canonical conditional rule(s).
They are enforced in addition to the property summary above.

### Model

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `upstream` | `Identifier` | Yes | — | — |
| `upstreamModel` | string | Yes | minimum length: `1`; maximum length: `256` | — |
| `pricingRef` | `Identifier` | No | — | — |
| `inputAccountingRef` | `Identifier` | No | — | — |
| `capabilities` | array of enum `"openai_responses"`, `"openai_chat"`, `"openai_embeddings"`, `"anthropic_messages"`, `"opaque_http"` | No | unique items | — |

### InputAccountingProfile

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `protocol` | enum `"openai_responses"`, `"openai_chat"`, `"openai_embeddings"`, `"anthropic_messages"` | Yes | — | — |
| `method` | constant `"utf8_byte_bpe_declared_framing_v1"` | Yes | — | — |
| `physicalModel` | string | Yes | minimum length: `1`; maximum length: `256` | — |
| `maximumFramingTokensPerRequest` | integer | Yes | minimum: `0`; maximum: `9223372036854775807` | — |
| `maximumFramingTokensPerMessage` | integer | Yes | minimum: `0`; maximum: `9223372036854775807` | — |
| `maximumContextTokens` | integer | Yes | minimum: `1`; maximum: `9223372036854775807` | — |

### PricingCatalog

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `currency` | constant `"USD"` | Yes | — | — |
| `effectiveAt` | string | No | format: `"date-time"` | — |
| `entries` | array of `PricingEntry` | Yes | minimum items: `1` | — |

### PricingEntry

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `model` | `Identifier` | Yes | — | — |
| `inputNanoUsdPerMillion` | integer | Yes | minimum: `0`; maximum: `9223372036854775807` | — |
| `outputNanoUsdPerMillion` | integer | Yes | minimum: `0`; maximum: `9223372036854775807` | — |
| `requestNanoUsd` | integer | No | minimum: `0`; maximum: `9223372036854775807`; default: `0` | — |

### LimitPlan

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `limits` | array of `Limit` | Yes | minimum items: `1`; maximum items: `128` | — |

### Limit

Shape: any of any JSON value / any JSON value / any JSON value. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `metric` | enum `"logical_requests"`, `"upstream_attempts"`, `"input_tokens"`, `"output_tokens"`, `"total_tokens"`, `"reasoning_tokens"`, `"cached_input_tokens"`, `"cache_write_tokens"`, `"cost_nano_usd"`, `"concurrent_requests"`, `"concurrent_streams"`, `"request_bytes"`, `"response_bytes"`, `"image_units"`, `"audio_seconds"`, `"tool_calls"` | Yes | — | — |
| `algorithm` | enum `"calendar"`, `"token_bucket"`, `"concurrency"`, `"per_request"` | No | — | — |
| `scope` | array of one of enum `"organization"`, `"application"`, `"environment"`, `"user"`, `"installation"`, `"installation_family"`, `"client_component"`, `"component_definition"`, `"component_kind"`, `"trust_source"`, `"feature"`, `"route"`, `"upstream"`, `"model"`, `"platform"` / string | No | minimum items: `1`; maximum items: `16`; unique items; contains string | Runtime validation canonicalizes the fixed dimensions and permits at most one normalized_claim:&lt;name&gt; selector. |
| `window` | string | No | pattern: `"^[1-9][0-9]*(m\|h\|d\|w\|mo)$"` | Server-owned calendar window: minute, hour, day, week, or month. Week windows begin Monday in the configured timezone. |
| `timezone` | string | No | pattern: `"^[A-Za-z][A-Za-z0-9._+-]*(/[A-Za-z0-9][A-Za-z0-9._+-]*)*$"`; minimum length: `1`; maximum length: `64`; default: `"UTC"`; includes a forbidden-shape rule | IANA timezone used only by calendar rules. The server defaults omitted values to UTC and never derives this from a client header. |
| `maximum` | integer | No | minimum: `1`; maximum: `9223372036854775807` | — |
| `perRequestMaximum` | integer | No | minimum: `1`; maximum: `9223372036854775807` | — |
| `capacity` | integer | No | minimum: `1`; maximum: `9223372036854775807` | — |
| `refillPerSecond` | number | No | exclusive minimum: `0` | — |
| `hard` | boolean | No | default: `true` | — |

### Feature

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `protocol` | enum `"openai_responses"`, `"openai_chat"`, `"openai_embeddings"`, `"anthropic_messages"`, `"opaque_http"` | Yes | — | — |
| `attestationPolicy` | `Identifier` | Yes | — | — |
| `access` | object | Yes | unknown properties rejected | — |
| `limitPlan` | object | Yes | unknown properties rejected | — |
| `output` | `OutputPolicy` | No | — | — |
| `routes` | array of `Route` | Yes | minimum items: `1`; maximum items: `32` | — |
| `opaqueHttp` | `OpaqueHttpPolicy` | No | — | — |

### OutputPolicy

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `defaultMaximumTokens` | integer | Yes | minimum: `1`; maximum: `10000000` | — |
| `absoluteMaximumTokens` | integer | Yes | minimum: `1`; maximum: `10000000` | — |

### Route

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `id` | `Identifier` | Yes | — | — |
| `when` | `CELExpression` | Yes | — | — |
| `model` | `Identifier` | Yes | — | — |
| `priority` | integer | Yes | minimum: `0`; maximum: `1000000` | — |
| `weight` | integer | No | minimum: `1`; maximum: `10000`; default: `1` | — |
| `stickyBy` | enum `"none"`, `"user"`, `"installation"` | No | default: `"none"` | — |
| `fallbackOn` | array of `RetryCondition` | No | unique items | — |
| `retryPolicy` | `RetryPolicy` | No | — | — |
| `maxRequestBodyBytes` | integer | No | minimum: `1`; maximum: `104857600` | Optional post-rewrite request-body bound for this route. It cannot exceed the server-wide request bound. |
| `maxRequestHeaderBytes` | integer | No | minimum: `1`; maximum: `32768` | Optional bound over the canonical parsed request headers, including Host and Latchway authentication/control fields. |
| `maxResponseBytes` | integer | No | minimum: `1`; maximum: `104857600` | Required for opaque_http features. The lower of this per-route bound and the server-wide response bound applies. |
| `timeouts` | `RouteTimeouts` | No | — | — |
| `streamingAllowed` | boolean | No | default: `false` | For opaque_http routes, permits text/event-stream responses. |
| `retryUnsafeMethods` | boolean | No | default: `false` | Explicit declaration that unsafe opaque methods are idempotent and may use this route's configured retries or fallback. |

### RetryCondition

Shape: enum `"connect_error"`, `"timeout_before_headers"`, `"first_byte_timeout"`, `"status_408"`, `"status_429"`, `"status_500"`, `"status_502"`, `"status_503"`, `"status_504"`. Definition constraints: —.

### RouteTimeouts

Shape: object. Definition constraints: minimum properties: `1`; unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `connect` | `Duration` | No | — | — |
| `responseHeader` | `Duration` | No | — | — |
| `firstByte` | `Duration` | No | — | — |
| `idle` | `Duration` | No | — | — |
| `total` | `Duration` | No | — | — |

### RetryPolicy

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `maxAttempts` | integer | Yes | minimum: `2`; maximum: `8` | Maximum attempts on this route, including the initial attempt. |
| `initialBackoffMilliseconds` | integer | No | minimum: `0`; maximum: `60000`; default: `0` | — |
| `maximumBackoffMilliseconds` | integer | No | minimum: `0`; maximum: `60000`; default: `0` | — |
| `jitterRatio` | number | No | minimum: `0`; maximum: `1`; default: `0` | Deterministic downward jitter as a fraction of the exponential backoff. |
| `retryOn` | array of `RetryCondition` | Yes | minimum items: `1`; unique items | — |

### OpaqueHttpPolicy

Closed opaque HTTP request policy. Exactly one of preferred pathTemplates or compatibility-only pathPrefixes is required.

Shape: object. Definition constraints: unknown properties rejected; 1 combined rule(s).

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `allowedMethods` | array of enum `"GET"`, `"POST"`, `"PUT"`, `"PATCH"`, `"DELETE"` | Yes | minimum items: `1`; maximum items: `5`; unique items | — |
| `pathPrefixes` | array of string | No | minimum items: `1`; maximum items: `32`; unique items; items: pattern: `"^/"`; maximum length: `512` | Compatibility-only v1 provider-relative segment-bound prefixes. Prefer pathTemplates for new configurations. |
| `pathTemplates` | array of string | No | minimum items: `1`; maximum items: `32`; unique items; items: pattern: `"^/"`; maximum length: `512` | Exact-depth provider-relative templates. A whole segment may be a unique {lower_snake_name} capture; catch-alls and overlapping templates are rejected. |
| `maxBodyBytes` | integer | Yes | minimum: `0`; maximum: `104857600` | — |
| `allowedRequestHeaders` | array of string | No | maximum items: `32`; unique items; items: pattern: `"^[A-Za-z0-9!#$%&'*+.^_`\|~-]+$"` | — |

This definition also has 1 canonical conditional rule(s).
They are enforced in addition to the property summary above.

### SessionPolicy

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `accessTokenTtl` | `Duration` | No | — | — |
| `challengeTtl` | `Duration` | No | — | — |
| `refreshTokenTtl` | `Duration` | No | — | — |
| `maximumClockSkewSeconds` | integer | No | minimum: `0`; maximum: `300` | — |

### PrivacyPolicy

Shape: object. Definition constraints: unknown properties rejected.

| Property | Shape | Required | Constraints | Notes |
| --- | --- | --- | --- | --- |
| `storePromptBodies` | boolean | No | default: `false` | Reserved for a future contract version. Latchway v1 rejects activation when this value is true. |
| `storeResponseBodies` | boolean | No | default: `false` | Reserved for a future contract version. Latchway v1 rejects activation when this value is true. |
| `bodyRetention` | `Duration` | No | — | Reserved duration for a future body-storage contract; it does not enable storage in v1. |

## Validation workflow

```bash
latchway config apply --environment env_... --file candidate.json --dry-run
latchway config apply --environment env_... --file candidate.json
latchway config validate rev_...
latchway config plan rev_...
```

A successful schema parse is only the first gate. Validation also resolves
references and secrets, compiles bounded CEL, proves protocol capabilities,
checks trusted input accounting and pricing, and rejects unsafe route or
quota combinations. See [Configuration](/administration/configuration) and
[CEL policy context](/reference/cel-policy-context).
