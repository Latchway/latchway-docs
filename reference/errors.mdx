---
title: "Errors"
description: "Handle every generated gateway Problem and SDK-local error with exact retry boundaries and remediation guidance."
icon: "circle-alert"
audience: "reference"
pageType: "reference"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-08-31"
owner: "core-api"
---

{/* Generated by scripts/public_reference.py. Do not edit. */}
{/* Canonical sources: api/error-codes.yaml and api/sdk-error-codes.yaml; registry 1; contract 1.0.0. */}

Latchway returns `application/problem+json`. The registry currently
defines 59 stable codes. Provider payloads, credentials,
identity tokens, proofs, and raw internal errors are excluded.

## Problem fields

| Class | Fields |
| --- | --- |
| Required | `type`, `title`, `status`, `detail`, `code`, `request_id`, `retryable`, `documentation_url` |
| Optional | `instance`, `retry_after`, `operation_id`, `feature`, `supported_protocol_versions`, `errors` |

`operation_id` is required only for `operation_indeterminate` and is forbidden for every other code.

<Warning>
  `retryable: true` does not authorize a blind replay. Respect
  `retry_after`, the SDK's pre-dispatch boundary, and the route/session
  contract. Reconcile indeterminate mutations from durable state.
</Warning>

## Gateway Problem registry

| Code | HTTP | Retryable | Title | Required action |
| --- | --- | --- | --- | --- |
| [`request_invalid`](/errors/request-invalid) | 400 | No | Invalid request | Correct the request using the field-level errors before retrying. |
| [`identity_token_missing`](/errors/identity-token-missing) | 401 | No | Identity token required | Obtain a current token from the configured application identity provider. |
| [`identity_token_invalid`](/errors/identity-token-invalid) | 401 | No | Identity token invalid | Reauthenticate with the application identity provider. |
| [`identity_token_expired`](/errors/identity-token-expired) | 401 | No | Identity token expired | Refresh or reacquire the application identity token. |
| [`identity_reauthentication_required`](/errors/identity-reauthentication-required) | 401 | No | Identity reauthentication required | Perform an interactive or provider-approved reauthentication. |
| [`attestation_required`](/errors/attestation-required) | 401 | No | Attestation required | Complete the configured platform attestation flow. |
| [`attestation_unsupported`](/errors/attestation-unsupported) | 400 | No | Attestation unsupported | Use a supported platform/provider or contact the application operator. |
| [`attestation_invalid`](/errors/attestation-invalid) | 401 | No | Attestation invalid | Recreate platform evidence from the active challenge; do not reuse evidence. |
| [`attestation_stale`](/errors/attestation-stale) | 401 | No | Attestation stale | Obtain fresh attestation evidence. |
| [`attestation_step_up_required`](/errors/attestation-step-up-required) | 401 | No | Stronger attestation required | Complete the stronger trust flow required by the feature policy. |
| [`dpop_missing`](/errors/dpop-missing) | 401 | No | DPoP proof required | Sign this request with the installation key. |
| [`dpop_invalid`](/errors/dpop-invalid) | 401 | No | DPoP proof invalid | Recreate the proof for the exact method, URI and access token. |
| [`dpop_replayed`](/errors/dpop-replayed) | 401 | No | DPoP proof replayed | Never reuse a proof; create one with a new unique jti. |
| [`dpop_nonce_required`](/errors/dpop-nonce-required) | 401 | Yes | DPoP nonce required | Create one new proof with the DPoP-Nonce response value. |
| [`session_expired`](/errors/session-expired) | 401 | Yes | Session expired | Refresh the Latchway session before creating a new application request. |
| [`session_revoked`](/errors/session-revoked) | 401 | No | Session revoked | Start a new session only after resolving the revocation reason. |
| [`refresh_token_reused`](/errors/refresh-token-reused) | 401 | No | Refresh token reuse detected | Clear local session state and perform a new challenge exchange. |
| [`installation_revoked`](/errors/installation-revoked) | 403 | No | Installation revoked | Contact the application operator or re-register when policy permits. |
| [`installation_family_revoked`](/errors/installation-family-revoked) | 403 | No | Installation family revoked | Clear every component session in the family and re-register only when policy permits. |
| [`installation_family_not_found`](/errors/installation-family-not-found) | 404 | No | Installation family not found | Start a new root-component registration flow for this application environment. |
| [`component_definition_not_found`](/errors/component-definition-not-found) | 404 | No | Component definition not found | Use a component definition published by the active environment configuration. |
| [`component_not_configured`](/errors/component-not-configured) | 422 | No | Component not configured | Ask the operator to configure this component definition before provisioning it. |
| [`component_not_provisioned`](/errors/component-not-provisioned) | 403 | No | Component not provisioned | Obtain a fresh parent-authorized component grant before creating a component session. |
| [`component_revoked`](/errors/component-revoked) | 403 | No | Component revoked | Clear only this component's local state and provision it again when policy permits. |
| [`component_key_invalid`](/errors/component-key-invalid) | 401 | No | Component key invalid | Use the non-exportable P-256 key bound to this component and recreate the DPoP proof. |
| [`component_key_replaced`](/errors/component-key-replaced) | 401 | No | Component key replaced | Clear stale component credentials and use the replacement key and session. |
| [`component_delegation_expired`](/errors/component-delegation-expired) | 401 | No | Component delegation expired | Ask the trusted parent component to provision a new bounded delegation. |
| [`component_feature_not_granted`](/errors/component-feature-not-granted) | 403 | No | Component feature not granted | Use only features in the component's sealed delegation or request a new grant. |
| [`component_parent_trust_expired`](/errors/component-parent-trust-expired) | 401 | No | Parent component trust expired | Refresh or re-attest the trusted parent before provisioning or refreshing this component. |
| [`component_direct_attestation_required`](/errors/component-direct-attestation-required) | 401 | No | Direct component attestation required | Complete the component definition's direct platform attestation flow. |
| [`containing_app_setup_required`](/errors/containing-app-setup-required) | 409 | No | Containing app setup required | Open and configure the trusted containing application before using this extension component. |
| [`framework_integration_unsupported`](/errors/framework-integration-unsupported) | 400 | No | Framework integration unsupported | Use a framework integration declared compatible by the gateway discovery document. |
| [`framework_version_unsupported`](/errors/framework-version-unsupported) | 400 | No | Framework version unsupported | Upgrade or select a framework version listed in the compatibility registry. |
| [`transport_destination_not_allowed`](/errors/transport-destination-not-allowed) | 403 | No | Transport destination not allowed | Send authenticated requests only to the exact configured Latchway origin and allowed path. |
| [`transport_request_not_replayable`](/errors/transport-request-not-replayable) | 409 | No | Transport request not replayable | Recreate the request body before retrying; the transport will not replay a consumed stream. |
| [`feature_not_found`](/errors/feature-not-found) | 404 | No | Feature not found | Use a feature configured for this application environment. |
| [`feature_not_allowed`](/errors/feature-not-allowed) | 403 | No | Feature not allowed | The verified principal or trust state does not meet feature policy. |
| [`model_not_allowed`](/errors/model-not-allowed) | 403 | No | Model not allowed | Do not select a physical model; use the configured application feature. |
| [`quota_exceeded`](/errors/quota-exceeded) | 429 | Yes | Quota exceeded | Wait until retry_after or request a different operator-assigned limit plan. |
| [`concurrency_exceeded`](/errors/concurrency-exceeded) | 429 | Yes | Concurrency limit exceeded | Wait for an active request or stream to finish. |
| [`output_limit_exceeded`](/errors/output-limit-exceeded) | 400 | No | Output limit exceeded | Reduce the requested output to the server-advertised feature limit. |
| [`pricing_unavailable`](/errors/pricing-unavailable) | 503 | Yes | Pricing unavailable | Retry after pricing is restored or ask the operator to configure a catalog. |
| [`route_not_found`](/errors/route-not-found) | 503 | Yes | No route available | Retry only according to retry_after; the operator must restore routing. |
| [`upstream_unavailable`](/errors/upstream-unavailable) | 503 | Yes | Upstream unavailable | Retry according to retry_after only when the request was not dispatched. |
| [`upstream_timeout`](/errors/upstream-timeout) | 504 | No | Upstream timeout | Do not automatically replay; the upstream may have received the request. |
| [`upstream_protocol_error`](/errors/upstream-protocol-error) | 502 | No | Upstream protocol error | Preserve the request ID and contact the operator; do not replay blindly. |
| [`configuration_invalid`](/errors/configuration-invalid) | 422 | No | Configuration invalid | Correct every reported schema, reference, policy, pricing or route error. |
| [`server_not_ready`](/errors/server-not-ready) | 503 | Yes | Server not ready | Retry according to retry_after after dependencies become ready. |
| [`protocol_version_unsupported`](/errors/protocol-version-unsupported) | 426 | No | Protocol version unsupported | Upgrade to one of supported_protocol_versions. |
| [`authentication_required`](/errors/authentication-required) | 401 | No | Administrator authentication required | Authenticate through the Admin API or supply a valid scoped API token. |
| [`permission_denied`](/errors/permission-denied) | 403 | No | Permission denied | Ask an owner to grant the required capability. |
| [`resource_not_found`](/errors/resource-not-found) | 404 | No | Resource not found | Verify the tenant-scoped resource identifier. |
| [`conflict`](/errors/conflict) | 409 | No | Resource conflict | Refresh the resource state and resolve the conflicting operation. |
| [`etag_required`](/errors/etag-required) | 428 | No | ETag required | Resubmit the mutation with the current If-Match value. |
| [`etag_mismatch`](/errors/etag-mismatch) | 412 | No | ETag mismatch | Fetch the latest revision, merge changes and resubmit with its ETag. |
| [`bootstrap_disabled`](/errors/bootstrap-disabled) | 409 | No | Bootstrap disabled | Sign in as an existing administrator; first-owner setup is permanently closed. |
| [`rate_limited`](/errors/rate-limited) | 429 | Yes | Rate limited | Retry after retry_after. |
| [`operation_indeterminate`](/errors/operation-indeterminate) | 503 | Yes | Operation outcome indeterminate | Preserve operation_id. Only a correlated succeeded audit event proves the original request committed; current state or a later conflict is insufficient. Reconcile the audit result before retrying, except for an explicitly documented idempotent operation. |
| [`internal_error`](/errors/internal-error) | 500 | No | Internal server error | Preserve the request ID for operator diagnosis; do not replay blindly. |

## SDK-local error registry

SDK-local errors are not HTTP Problems and therefore have no canonical
HTTP status or global retryable flag. Honor the concrete error instance
and the bounded guidance below.

| Code | SDKs | Title | Required action |
| --- | --- | --- | --- |
| [`client_configuration_invalid`](/errors/client-configuration-invalid) | Javascript, React Native | Client configuration invalid | Correct the SDK configuration before creating another client or request. Never weaken exact-origin, identity, attestation, or key-storage checks to bypass this error. |
| [`storage_unavailable`](/errors/storage-unavailable) | Javascript, React Native | Secure client storage unavailable | Restore the platform storage capability required by the configured persistence mode, or choose an explicitly supported non-persistent mode when the application threat model permits it. |
| [`crypto_unavailable`](/errors/crypto-unavailable) | Javascript, React Native | Client cryptography unavailable | Use a supported runtime with the required P-256 signing and secure-random capabilities. Do not replace the installation key with an exportable application secret. |
| [`attestation_provider_missing`](/errors/attestation-provider-missing) | Javascript, React Native | Attestation provider missing | Configure the platform attestation adapter required by the environment and obtain fresh evidence for the active challenge. |
| [`protocol_response_invalid`](/errors/protocol-response-invalid) | Javascript, React Native | Protocol response invalid | Preserve redaction-safe request metadata and upgrade or repair the gateway or SDK contract mismatch. Do not consume credentials or payload fields from the rejected response. |
| [`request_not_replayable`](/errors/request-not-replayable) | Javascript, React Native | Client request not replayable | Recreate the request with a fresh body before an explicit retry. The SDK will not replay a body that was consumed or cannot be safely buffered. |
| [`network_error`](/errors/network-error) | Javascript, React Native | Client network error | Check connectivity and the exact configured Latchway origin. Retry only when the error instance marks the operation retryable and the request body remains replayable. |
| [`root_keychain_migration_required`](/errors/root-keychain-migration-required) | Ios | Root Keychain migration required | On a disposable development device, reset the stale Latchway Keychain state or use a new test bundle identifier, then reinstall with the private root access group. Production migrations require an explicit reviewed migration path. |
| [`key_unavailable`](/errors/key-unavailable) | Ios, Android | Installation key unavailable | Restore the required hardware-backed key capability or apply the environment's explicit fallback policy. Never export or substitute a provider credential. |
| [`session_unavailable`](/errors/session-unavailable) | Ios | Client session unavailable | Re-establish identity and attestation, then create a fresh Latchway session. Do not continue with stale access or refresh credentials. |
| [`transport_failure`](/errors/transport-failure) | Ios | Client transport failure | Check connectivity and the exact configured gateway origin. Retry only across the SDK's documented pre-dispatch boundary with a replayable request. |
| [`server_response_invalid`](/errors/server-response-invalid) | Ios | Server response invalid | Treat the response as untrusted, preserve its redaction-safe request identifier when available, and repair the gateway or SDK contract mismatch before retrying. |
| [`cancelled`](/errors/cancelled) | Ios | Client operation cancelled | Treat cancellation as a caller-controlled terminal result. Start a new operation only if the application still needs it; never infer whether an already-dispatched mutation committed. |
| [`keychain_access_group_unavailable`](/errors/keychain-access-group-unavailable) | Ios | Keychain access group unavailable | Add the required containing-app and extension Keychain Sharing entitlements, use the exact application-prefix-qualified group, and regenerate signing profiles before provisioning the component again. |
| [`foundation_models_invalid_transcript`](/errors/foundation-models-invalid-transcript) | Ios | Foundation Models transcript invalid | Supply a transcript with translatable text input. Unsupported or empty transcript content is rejected instead of being silently changed. |
| [`foundation_models_sampling_unsupported`](/errors/foundation-models-sampling-unsupported) | Ios | Foundation Models sampling unsupported | Use the adapter's declared sampling capabilities. The adapter does not silently translate unsupported Foundation Models sampling modes. |
| [`foundation_models_gateway_error`](/errors/foundation-models-gateway-error) | Ios | Foundation Models gateway error | Inspect the associated HTTP status and redaction-safe request identifier, then follow the canonical gateway error and replay guidance for that request. |
| [`foundation_models_gateway_stream_invalid`](/errors/foundation-models-gateway-stream-invalid) | Ios | Foundation Models gateway stream invalid | Discard the incomplete output and repair the Responses streaming contract mismatch. Do not treat a partial stream as a completed model response. |
| [`secure_state_unavailable`](/errors/secure-state-unavailable) | Android | Secure client state unavailable | Restore the configured Android secure-state and keystore capabilities before creating another session. Do not persist Latchway credentials in ordinary application storage. |
| [`network_unavailable`](/errors/network-unavailable) | Android | Native network unavailable | Check connectivity and the exact configured Latchway origin. Retry only when the exception is explicitly retryable and the request remains replayable. |
| [`response_invalid`](/errors/response-invalid) | Android | Native response invalid | Treat the native or gateway response as untrusted and repair the contract mismatch before retrying or consuming any response data. |

## Direct diagnostic links

Each stable code has a deterministic heading so SDK errors and request
views can link to the exact action.

### `request_invalid`

**HTTP 400 · Retryable: No**

Correct the request using the field-level errors before retrying.

### `identity_token_missing`

**HTTP 401 · Retryable: No**

Obtain a current token from the configured application identity provider.

### `identity_token_invalid`

**HTTP 401 · Retryable: No**

Reauthenticate with the application identity provider.

### `identity_token_expired`

**HTTP 401 · Retryable: No**

Refresh or reacquire the application identity token.

### `identity_reauthentication_required`

**HTTP 401 · Retryable: No**

Perform an interactive or provider-approved reauthentication.

### `attestation_required`

**HTTP 401 · Retryable: No**

Complete the configured platform attestation flow.

### `attestation_unsupported`

**HTTP 400 · Retryable: No**

Use a supported platform/provider or contact the application operator.

### `attestation_invalid`

**HTTP 401 · Retryable: No**

Recreate platform evidence from the active challenge; do not reuse evidence.

### `attestation_stale`

**HTTP 401 · Retryable: No**

Obtain fresh attestation evidence.

### `attestation_step_up_required`

**HTTP 401 · Retryable: No**

Complete the stronger trust flow required by the feature policy.

### `dpop_missing`

**HTTP 401 · Retryable: No**

Sign this request with the installation key.

### `dpop_invalid`

**HTTP 401 · Retryable: No**

Recreate the proof for the exact method, URI and access token.

### `dpop_replayed`

**HTTP 401 · Retryable: No**

Never reuse a proof; create one with a new unique jti.

### `dpop_nonce_required`

**HTTP 401 · Retryable: Yes**

Create one new proof with the DPoP-Nonce response value.

### `session_expired`

**HTTP 401 · Retryable: Yes**

Refresh the Latchway session before creating a new application request.

### `session_revoked`

**HTTP 401 · Retryable: No**

Start a new session only after resolving the revocation reason.

### `refresh_token_reused`

**HTTP 401 · Retryable: No**

Clear local session state and perform a new challenge exchange.

### `installation_revoked`

**HTTP 403 · Retryable: No**

Contact the application operator or re-register when policy permits.

### `installation_family_revoked`

**HTTP 403 · Retryable: No**

Clear every component session in the family and re-register only when policy permits.

### `installation_family_not_found`

**HTTP 404 · Retryable: No**

Start a new root-component registration flow for this application environment.

### `component_definition_not_found`

**HTTP 404 · Retryable: No**

Use a component definition published by the active environment configuration.

### `component_not_configured`

**HTTP 422 · Retryable: No**

Ask the operator to configure this component definition before provisioning it.

### `component_not_provisioned`

**HTTP 403 · Retryable: No**

Obtain a fresh parent-authorized component grant before creating a component session.

### `component_revoked`

**HTTP 403 · Retryable: No**

Clear only this component's local state and provision it again when policy permits.

### `component_key_invalid`

**HTTP 401 · Retryable: No**

Use the non-exportable P-256 key bound to this component and recreate the DPoP proof.

### `component_key_replaced`

**HTTP 401 · Retryable: No**

Clear stale component credentials and use the replacement key and session.

### `component_delegation_expired`

**HTTP 401 · Retryable: No**

Ask the trusted parent component to provision a new bounded delegation.

### `component_feature_not_granted`

**HTTP 403 · Retryable: No**

Use only features in the component's sealed delegation or request a new grant.

### `component_parent_trust_expired`

**HTTP 401 · Retryable: No**

Refresh or re-attest the trusted parent before provisioning or refreshing this component.

### `component_direct_attestation_required`

**HTTP 401 · Retryable: No**

Complete the component definition's direct platform attestation flow.

### `containing_app_setup_required`

**HTTP 409 · Retryable: No**

Open and configure the trusted containing application before using this extension component.

### `framework_integration_unsupported`

**HTTP 400 · Retryable: No**

Use a framework integration declared compatible by the gateway discovery document.

### `framework_version_unsupported`

**HTTP 400 · Retryable: No**

Upgrade or select a framework version listed in the compatibility registry.

### `transport_destination_not_allowed`

**HTTP 403 · Retryable: No**

Send authenticated requests only to the exact configured Latchway origin and allowed path.

### `transport_request_not_replayable`

**HTTP 409 · Retryable: No**

Recreate the request body before retrying; the transport will not replay a consumed stream.

### `feature_not_found`

**HTTP 404 · Retryable: No**

Use a feature configured for this application environment.

### `feature_not_allowed`

**HTTP 403 · Retryable: No**

The verified principal or trust state does not meet feature policy.

### `model_not_allowed`

**HTTP 403 · Retryable: No**

Do not select a physical model; use the configured application feature.

### `quota_exceeded`

**HTTP 429 · Retryable: Yes**

Wait until retry_after or request a different operator-assigned limit plan.

### `concurrency_exceeded`

**HTTP 429 · Retryable: Yes**

Wait for an active request or stream to finish.

### `output_limit_exceeded`

**HTTP 400 · Retryable: No**

Reduce the requested output to the server-advertised feature limit.

### `pricing_unavailable`

**HTTP 503 · Retryable: Yes**

Retry after pricing is restored or ask the operator to configure a catalog.

### `route_not_found`

**HTTP 503 · Retryable: Yes**

Retry only according to retry_after; the operator must restore routing.

### `upstream_unavailable`

**HTTP 503 · Retryable: Yes**

Retry according to retry_after only when the request was not dispatched.

### `upstream_timeout`

**HTTP 504 · Retryable: No**

Do not automatically replay; the upstream may have received the request.

### `upstream_protocol_error`

**HTTP 502 · Retryable: No**

Preserve the request ID and contact the operator; do not replay blindly.

### `configuration_invalid`

**HTTP 422 · Retryable: No**

Correct every reported schema, reference, policy, pricing or route error.

### `server_not_ready`

**HTTP 503 · Retryable: Yes**

Retry according to retry_after after dependencies become ready.

### `protocol_version_unsupported`

**HTTP 426 · Retryable: No**

Upgrade to one of supported_protocol_versions.

### `authentication_required`

**HTTP 401 · Retryable: No**

Authenticate through the Admin API or supply a valid scoped API token.

### `permission_denied`

**HTTP 403 · Retryable: No**

Ask an owner to grant the required capability.

### `resource_not_found`

**HTTP 404 · Retryable: No**

Verify the tenant-scoped resource identifier.

### `conflict`

**HTTP 409 · Retryable: No**

Refresh the resource state and resolve the conflicting operation.

### `etag_required`

**HTTP 428 · Retryable: No**

Resubmit the mutation with the current If-Match value.

### `etag_mismatch`

**HTTP 412 · Retryable: No**

Fetch the latest revision, merge changes and resubmit with its ETag.

### `bootstrap_disabled`

**HTTP 409 · Retryable: No**

Sign in as an existing administrator; first-owner setup is permanently closed.

### `rate_limited`

**HTTP 429 · Retryable: Yes**

Retry after retry_after.

### `operation_indeterminate`

**HTTP 503 · Retryable: Yes**

Preserve operation_id. Only a correlated succeeded audit event proves the original request committed; current state or a later conflict is insufficient. Reconcile the audit result before retrying, except for an explicitly documented idempotent operation.

### `internal_error`

**HTTP 500 · Retryable: No**

Preserve the request ID for operator diagnosis; do not replay blindly.

### `client_configuration_invalid`

**SDK-local · Javascript, React Native**

Correct the SDK configuration before creating another client or request. Never weaken exact-origin, identity, attestation, or key-storage checks to bypass this error.

### `storage_unavailable`

**SDK-local · Javascript, React Native**

Restore the platform storage capability required by the configured persistence mode, or choose an explicitly supported non-persistent mode when the application threat model permits it.

### `crypto_unavailable`

**SDK-local · Javascript, React Native**

Use a supported runtime with the required P-256 signing and secure-random capabilities. Do not replace the installation key with an exportable application secret.

### `attestation_provider_missing`

**SDK-local · Javascript, React Native**

Configure the platform attestation adapter required by the environment and obtain fresh evidence for the active challenge.

### `protocol_response_invalid`

**SDK-local · Javascript, React Native**

Preserve redaction-safe request metadata and upgrade or repair the gateway or SDK contract mismatch. Do not consume credentials or payload fields from the rejected response.

### `request_not_replayable`

**SDK-local · Javascript, React Native**

Recreate the request with a fresh body before an explicit retry. The SDK will not replay a body that was consumed or cannot be safely buffered.

### `network_error`

**SDK-local · Javascript, React Native**

Check connectivity and the exact configured Latchway origin. Retry only when the error instance marks the operation retryable and the request body remains replayable.

### `root_keychain_migration_required`

**SDK-local · Ios**

On a disposable development device, reset the stale Latchway Keychain state or use a new test bundle identifier, then reinstall with the private root access group. Production migrations require an explicit reviewed migration path.

### `key_unavailable`

**SDK-local · Ios, Android**

Restore the required hardware-backed key capability or apply the environment's explicit fallback policy. Never export or substitute a provider credential.

### `session_unavailable`

**SDK-local · Ios**

Re-establish identity and attestation, then create a fresh Latchway session. Do not continue with stale access or refresh credentials.

### `transport_failure`

**SDK-local · Ios**

Check connectivity and the exact configured gateway origin. Retry only across the SDK's documented pre-dispatch boundary with a replayable request.

### `server_response_invalid`

**SDK-local · Ios**

Treat the response as untrusted, preserve its redaction-safe request identifier when available, and repair the gateway or SDK contract mismatch before retrying.

### `cancelled`

**SDK-local · Ios**

Treat cancellation as a caller-controlled terminal result. Start a new operation only if the application still needs it; never infer whether an already-dispatched mutation committed.

### `keychain_access_group_unavailable`

**SDK-local · Ios**

Add the required containing-app and extension Keychain Sharing entitlements, use the exact application-prefix-qualified group, and regenerate signing profiles before provisioning the component again.

### `foundation_models_invalid_transcript`

**SDK-local · Ios**

Supply a transcript with translatable text input. Unsupported or empty transcript content is rejected instead of being silently changed.

### `foundation_models_sampling_unsupported`

**SDK-local · Ios**

Use the adapter's declared sampling capabilities. The adapter does not silently translate unsupported Foundation Models sampling modes.

### `foundation_models_gateway_error`

**SDK-local · Ios**

Inspect the associated HTTP status and redaction-safe request identifier, then follow the canonical gateway error and replay guidance for that request.

### `foundation_models_gateway_stream_invalid`

**SDK-local · Ios**

Discard the incomplete output and repair the Responses streaming contract mismatch. Do not treat a partial stream as a completed model response.

### `secure_state_unavailable`

**SDK-local · Android**

Restore the configured Android secure-state and keystore capabilities before creating another session. Do not persist Latchway credentials in ordinary application storage.

### `network_unavailable`

**SDK-local · Android**

Check connectivity and the exact configured Latchway origin. Retry only when the exception is explicitly retryable and the request remains replayable.

### `response_invalid`

**SDK-local · Android**

Treat the native or gateway response as untrusted and repair the contract mismatch before retrying or consuming any response data.

For `operation_indeterminate`, retain the operation ID and request ID,
query audit or the affected resource, and decide from durable state before
another mutation. For terminal component or family codes, clear only the
credential scope named by the code unless family revocation is explicit.
