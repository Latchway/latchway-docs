---
title: "Build with the React Native SDK"
description: "Choose the TypeScript, iOS host, Android host, streaming, framework, extension, and release tasks for React Native."
icon: "atom"
audience: "client-react-native"
pageType: "how-to"
serverVersion: "1.0.0"
sdkVersion: "1.0.0"
lastVerified: "2026-08-31"
owner: "react-native-sdk"
---

<Badge color="yellow">Source candidate 1.0.0</Badge> <Badge color="blue">React Native 0.82</Badge>

The React Native API is TypeScript, but the security boundary is native. The
iOS or Android module owns the component key, App Attest or Play Integrity,
DPoP signing, rotating refresh state, and protected network request.

## Start here

<CardGroup cols={2}>
  <Card title="Complete the quickstart" icon="send" href="/clients/react-native/quickstart">
    Use one TypeScript API and verify one request from each native platform.
  </Card>
  <Card title="Install the source workspace" icon="package" href="/clients/react-native/installation">
    Resolve the exact JavaScript, Swift, Android, TurboModule, and contract locks.
  </Card>
  <Card title="Configure native hosts" icon="settings" href="/clients/react-native/ios-native-setup">
    Begin with iOS App Attest, or open the Android host setup from that guide.
  </Card>
  <Card title="Prepare a release" icon="list-checks" href="/clients/react-native/production-checklist">
    Prove both native runtime paths without moving security state into JavaScript.
  </Card>
</CardGroup>

## Runtime boundary

```text
TypeScript API -> TurboModule -> native iOS or Android SDK -> Latchway
                   no keys, proofs, sessions, or raw attestation returned
```

- [Authentication](/clients/react-native/authentication) passes the existing
  application identity token into the native operation only when needed.
- [Send a request](/clients/react-native/send-a-request) covers the bounded
  feature-aware fetch surface.
- [Streaming](/clients/react-native/streaming) covers pull-driven responses and
  `AbortSignal` cancellation.
- [Framework integrations](/clients/react-native/framework-integrations)
  separates tested custom-fetch consumers from browser-only or Node-only paths.
- [App extensions](/clients/react-native/app-extensions) records the current
  delegated-component diagnostic boundary without claiming request execution.
- [Error handling](/clients/react-native/error-handling) keeps stable error and
  operation IDs safe across the bridge.

## Support boundary

The source candidate supports React Native 0.82 with the New Architecture,
iOS 15+, and Android API 24+. Legacy Architecture and Expo-managed support are
not inferred. The npm and native dependency artifacts remain unpublished until
[Release status](/release-status) records promotion. Consult the generated
[React Native SDK bundle](/reference/sdk-bundles/react-native) for exact source
and version evidence.

<Warning>
  React Native and Web share JavaScript syntax but not a trust model. Do not
  replace the native module with WebCrypto, IndexedDB, App Check, Turnstile, or
  a browser client inside a native application.
</Warning>
